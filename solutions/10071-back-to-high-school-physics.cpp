#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>

int main() {
    int v, t, s;
    float a;
    while (std::cin >> v >> t) {
        std::cout << 2 * v * t << std::endl;
    }

    return 0;
}
